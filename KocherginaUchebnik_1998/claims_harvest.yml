# claims_harvest.yml — bulk harvested falsifiable-assertion BACKLOG for KocherginaUchebnik_1998
#
# Companion to claims.yml (the curated, VERIFIED register). This file WAS the full-textbook
# harvest (H768 scale-out, 12-07-2026): 223 falsifiable grammatical assertions swept from the
# 15,756-line mdx by 6 parallel readers, in reading order, each tagged kochergina_gives_number.
#
# STATUS: FULLY DRAINED (H797, 12-07-2026). Every candidate has been verdicted on the two axes
# (verdict_fact + verdict_pedagogy), given its number (systemic Whitney or reproducible DCS-2021),
# an M.G. frequency footnote where Kochergina gives none and the corpus supplies one, and
# PROMOTED into claims.yml (HK-1..HK-233). The backlog is now empty — see claims.yml for the
# 234-claim verified register and CLAIMS_VERIFIED.md for the rendered two-axis table.
#
# build_claims.py still reads this file and renders any remaining candidates in a "Harvest
# backlog" section beneath the verified table; with the list empty, no backlog section is shown.
# If a future re-sweep surfaces new candidates, append them here (same schema) and re-run the
# verifier -> promote loop.
#
# kochergina_gives_number: true  = she states a quantity/frequency (verify it)
#                          false = she gives none (add it as an M.G. footnote where computable)

work: "Kochergina, Учебник санскрита (Sanskrit textbook), 1998 — harvest backlog (fully drained, H797)"

candidates: []
