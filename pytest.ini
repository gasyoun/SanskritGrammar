[pytest]
# Unit tests for the Python build scripts under scripts/. tests/conftest.py puts
# scripts/ on sys.path so the standalone scripts import as modules.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
