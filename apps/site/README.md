# Site application seam

The root Docusaurus entry points remain runnable during Slice A. New typed site
configuration and components belong here; existing JSX migrates only when
touched, avoiding a mass conversion.

