# Site application seam

The root Docusaurus entry points remain runnable during Slice A. New typed site
configuration and components belong here; the existing `src/` tree is migrated
only when touched, avoiding a mass JSX conversion.

