---
title: Переиспользование активов
sidebar_label: Переиспользование
sidebar_position: 4
slug: /sangram/atlas/reuse
description: Представление переиспользования публичного атласа Sangram — у кого уже есть нужный актив, кто его потребляет и что запрещено пересоздавать.
---

import AtlasReuseView from '@site/src/components/AtlasReuseView';
import bundle from './data/atlas.bundle.json';

# Переиспользование готовых активов

_Создано: 12-07-2026 · Последнее обновление: 12-07-2026_

Второе из пяти представлений [атласа Sangram](https://gasyoun.github.io/SanskritGrammar/grammars/sangram/atlas)
(слот B3 серии). Оно отвечает на один вопрос:
**у кого уже есть нужный актив, кто его уже потребляет и что именно запрещено
пересоздавать**. Каждое семейство активов — код, данные, схема или workflow —
имеет ровно одного канонического владельца; потребители запрашивают или
импортируют его выход, а не выводят собственную копию. Каждый запрет ниже
оплачен уже случившимся дублированием, измеренным во внутреннем census
организации, — не абстрактной осторожностью.

Представление читает только санитизированный публичный bundle по
[контракту данных](https://gasyoun.github.io/SanskritGrammar/grammars/sangram/atlas/data-contract)
(слот B1): приватные ярусы и летучие статусы туда не попадают по построению,
внутренние источники называются по имени, но не адресуются.

```mermaid
flowchart LR
  accTitle: Схема чтения представления переиспользования
  accDescr: Цепочка из четырех звеньев. Репозиторий-владелец владеет каноническим семейством активов. Семейство несет запрет пересоздания и ярус прав — открытый, ограниченный правами или карантин. Актив питает репозитории-потребители и учебные поверхности. Отдельная ветвь — внешние стеки, которые вызываются или зеркалируются, но не клонируются. Примеры: sanskrit-util владеет транскодированием, которое питает пять потребителей; csl-orig владеет текстами словарей CDSL, которые читает весь словарный слой; vidyut вендорится как движок флексии.
  subgraph CHAIN["Цепочка: владелец → актив → потребитель"]
    direction LR
    OWN["Репозиторий-владелец"] -->|"owns"| AST["Каноническое семейство<br/>+ запрет пересоздания<br/>+ ярус прав ⚖️/🧪"]
    AST -->|"feeds"| CONS["Репозитории-потребители<br/>и учебные поверхности"]
  end
  subgraph EXAMPLES["Три примера из bundle"]
    direction TB
    U["sanskrit-util"] -->|"owns"| T["Транскодирование IAST⇄SLP1 🧪"]
    T -->|"feeds"| TC["csl-apidev · csl-guides · csl-atlas<br/>· WhitneyRoots · SanskritSpellCheck"]
    O["csl-orig"] -->|"owns"| S["Тексты словарей CDSL"]
    S -->|"feeds + consumes"| SC["весь словарный слой (31+ репо)"]
    XV["внешний стек vidyut"] -->|"vendors"| K["kosha (движок флексии)"]
  end
  classDef owner fill:#f1f3f5,stroke:#495057,color:#1a1a1a
  classDef asset fill:#e7f5ff,stroke:#1971c2,color:#1a1a1a
  classDef warn fill:#fff3bf,stroke:#e67700,color:#1a1a1a
  classDef cons fill:#f8f9fa,stroke:#868e96,color:#1a1a1a
  class OWN,U,O owner
  class AST,S asset
  class T,XV,K warn
  class CONS,TC,SC cons
```

Как читать карточки ниже: **владелец** — единственный канонический дом
семейства; **«не пересоздавать»** — предупреждение, зачем нельзя строить
заново; **ярус прав** — ⚖️ ограничение правами (лицензия или закрытый ярус
данных за публичным builder'ом), 🧪 карантин (кандидатный слой, потребляемый
только с явной меткой), без маркера — публичный воспроизводимый актив.
Маршрут при новой задаче: это представление говорит, *кто владеет и что
запрещено*; что именно импортировать — внутренние реестры организации,
названные в свидетельствах карточек.

<AtlasReuseView bundle={bundle} />

_Dr. Mārcis Gasūns_
