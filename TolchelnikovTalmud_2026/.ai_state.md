# Project Objective: Talmud санскрита — interactive Docusaurus companion to Zalizniak's Очерк (1978), per [IMPROVEMENT_PLAN.md](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/IMPROVEMENT_PLAN.md)

## ➡️ Next Steps (Queue)
- **Next: H241** — remaining judgment passes (Opus 4.8 `claude-opus-4-8`): self-study
  re-scaffold + §0 notation onboarding (Phase 1), exercise answer keys (Phase 4),
  glossary + include wiring (Phase 5). Phase 0 scaffold, Phase 2 widgets, Phase 3
  crosswalk, and the **Zalizniak §-concordance (Phase 0/1 spine) are DONE**. Start line
  lives in
  [H241](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H241-Opus_TolchelnikovTalmud_2026_interactive_companion_buildout_06.07.26.md).
- Every `TODO(H241)` marker in this folder is an H241 work item — grep for it.
- **NEW workstream — `samskrtam.ru/z/` integration (MG-approved 07-07-2026).** `/z/` is
  **not** a Zalizniak-Очерк edition (as the H241 handoff assumed) — it is Shirobokov's
  interactive DB of *Tolchelnikov's* «Санскритская морфология» (the Talmud itself), one
  root per `/z/verb.php?id=N`, each carrying an authoritative **Ряд + seṭ + class +
  «Корень по Whitney» + full generated paradigms**. MG wants (a) **reuse** `/z/`'s Ряд/seṭ
  to replace our *derived/advisory* values in
  [`data/whitney_talmud.json`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/whitney_talmud.json)
  (shrinking the pending footnote-proposal queue) and (b) **deep-link** Appendix 1 rows +
  widget roots to `/z/verb.php?id=N`. Both need a harvested **Whitney-no ↔ /z/ id map**
  (scrape the `/z/` index + verb pages; MG flags `/z/` as "not very clean"). Deploy target
  is **samskrtam.ru** (affects baseUrl/cross-linking — a Phase-5 concern). Handoff
  [H329](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H329-Opus_SanskritGrammar_talmud_z_root_db_integration_07.07.26.md).
  **Progress 08-07-2026 (Opus 4.8 `claude-opus-4-8`):** Phases 1/2 (harvest + reconcile,
  [PR #44](https://github.com/gasyoun/SanskritGrammar/pull/44)) and Phase 4 (deep-links,
  [PR #45](https://github.com/gasyoun/SanskritGrammar/pull/45)) **DONE** — the `/z/` index
  page alone (one polite fetch, 905 rows) sufficed, no detail scrape.
  [`data/z_root_map.json`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/z_root_map.json)
  (876/905 matched) + [`data/z_reconciliation_report.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/z_reconciliation_report.md):
  Ряд agree **70.7%**, seṭ **91.3%**, `/z/` fills **246** of our null seṭ + flags **203 veṭ**;
  found a systematic ṛ-nucleus R→A divergence + 57 false-`high` derived Ряд. Deep-links live
  in Appendix 1 («/z/» column) + AblautMachine/SetTree captions (39/44 + 101/106 covered).
  **Phase 3 REMAINING — REFRAMED by author ruling (Tolchelnikov-Ivan, [issue #50](https://github.com/gasyoun/SanskritGrammar/issues/50), 08-07-2026):**
  Ряд/seṭ/Whitney-refs come from the **latest manual = [`Talmud-2.1.6.mdx`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/Talmud-2.1.6.mdx)**
  (the current authoritative edition), **not** `/z/` and **not** our derivation. So Phase 3 = **transcribe
  the manual's own Приложение 1 catalog** into `whitney_talmud.json` (un-indexed rows stay un-indexed;
  no `0`-variants), **NO methodology footnote** (FN-0001/0002 already marked `rejected`), Whitney refs
  from the manual (roots absent from Whitney carry none). `/z/` stays only as the paradigm deep-link
  (outdated snapshot, still useful). No `/review-sheet` needed — the author has ruled.
  **Phase 3 ✅ DONE 08-07-2026** — see ✅ Completed. Remaining H329 refinement: none blocking.

## 🚧 Current Work-In-Progress (WIP)
- None active. **Phase 0 scaffold, Phase 2 widgets, Phase 3 crosswalk, and the Zalizniak
  §-concordance are landed.** Remaining H241 phases (one PR per phase): **Phase 1**
  self-study re-scaffold + §0 notation onboarding, **Phase 4** answer-key fill in
  [`Talmud-uroky.mdx`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/Talmud-uroky.mdx),
  **Phase 5** glossary + include wiring.
- **Author-gating open:** FN-0003…0014 freq-notes in
  [`footnote-proposals/proposals.yml`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/footnote-proposals/proposals.yml)
  are `status: pending` — surface to Ivan via `/review-sheet` before any enters the
  running text. FN-0001/0002 (derived Ряд/seṭ methodology) are `rejected` per the
  issue-#50 ruling and now moot (Ряд/seṭ are the author's own manual values, H329 Phase 3).

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)
- **Standing rule (MG, 08-07-2026): keep [`CHANGELOG.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/CHANGELOG.md)
  updated for EVERY Talmud change** (text, data, widgets, apparatus) — add an `[Unreleased]`
  entry in the same pass, no silent edits.
- **`/z/` version drift (MG, 08-07-2026):** [samskrtam.ru/z/](https://samskrtam.ru/z/) was
  generated from an **earlier Talmud version** than the current v2.1.6, so some Ряд/seṭ
  discrepancies (see [`data/z_reconciliation_report.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/z_reconciliation_report.md))
  are **authorial version drift**, not errors. Phase 3 must weigh three causes per conflict —
  `/z/` bug · our-derivation error · version drift — not assume `/z/` is authoritative.
- The split pages live flat in this folder root (the docs `include` glob
  `TolchelnikovTalmud_2026/**.mdx` + autogenerated sidebar pick them up; positions 1–15
  via frontmatter). `Talmud-2.1.6.mdx` is the raw Pandoc dump — archive only, left
  untouched; `talmud-refactoring.md` is the clean source of the split, do not edit the
  split pages without keeping it in sync.
- MDX safety fixes applied during the split: `<br>` → `<br />` (Table 4, §III) and raw
  `{A}`/`{B}` outside code spans wrapped in backticks (§III) — everything else was
  already brace-safe (all `{…}` notation sits inside backticks).
- `.gitignore` uses an ignore-all-except allow-list for this folder — every new tracked
  file must be added there explicitly (errata.yml, ERRATA.md, .ai_state.md,
  footnote-proposals/ are in as of H242).
- **⚠ Pre-existing site-wide build break (found 07-07-2026, H241 concordance pass):**
  `npm run build` currently fails SSG for **every** page under the `grammars` docs
  instance — 28 pages, `TypeError: Cannot read properties of undefined (reading 'id')`
  in the theme-classic `DocItem` component — hitting ApteSyntax, BuhlerLeitfaden,
  GasunsDhatu, KnauerFrazy, Zalizniak, ALL Talmud pages, incl. untouched files
  (`Talmud-2.1.6`, `talmud-zaklyuchenie`). Confirmed identical with the H241 changeset
  fully stashed, so it is **not** an H241 regression — the MDX compile phase passes
  clean (no `Module build failed`), the crash is at static-render. Likely a
  theme/doc-id/node_modules-state issue on `chore/errata-kochergina-waiting`; Phase 2's
  "build passes" note predates it. **Fix is out of scope for the concordance PR** — flag
  for a dedicated Docusaurus-build-repair pass before the site is deployed.

## ✅ Completed (Recent only)
- **Поз.1↔3 grade-mapping inversion + Тип/seṭ code mix-up, fixed (15-07-2026, Sonnet 5
  `claude-sonnet-5`).** Surfaced by the H995 agent while filling Talmud-uroky.mdx answer
  keys (PR #232, still open/unmerged — separate concern). `talmud-02-cheredovanie.mdx`,
  `onramp/step-2-pozicia-i-tip.mdx`, and the `AblautMachine` free-exploration caption all
  had Поз.1→Вриддхи/Поз.3→Слабая (manual's own Table 5 says the reverse: Поз.1→Слабая,
  Поз.3→Вриддхи, in all four types) and mislabeled the ablaut-Тип rows as `s`/`a`/`v`/`v1`–`v4`
  (those are Table 8's seṭ/aniṭ/veṭ codes — a **different** dimension; real Тип labels are
  `I`/`II`/`III`/`IV`). This exact conflation was already flagged once, in the H329 Phase-3
  note below ("the prior split-page legend... had conflated Тип with the seṭ codes") — the
  data pipeline (`tip` field) was corrected then, but these three prose/caption spots were
  missed and stayed wrong until now. Also found and fixed the same leftover in
  `tools/build_whitney_talmud.py`'s `tip_default` fallback (was `"s"`, outside `tip`'s own
  `I`/`II`/`III`/`IV`/null value space — changed to `"I"`); regenerated
  `data/whitney_talmud.json` (930 records) and `data/widget_roots.json` (no diff — confirmed
  `tip_default` has zero downstream consumers, so this was purely a data-correctness fix,
  not a functional one). `npm run build` green. PR: see repo pull requests for
  `fix-talmud-position-grade-inversion`.
- **H329 Phase 3 — manual is the source of truth for Ряд/Тип/seṭ (08-07-2026, Opus 4.8
  `claude-opus-4-8`).** Per the author's [issue #50](https://github.com/gasyoun/SanskritGrammar/issues/50)
  ruling, parsed the author's own **Приложение 1** catalog (745 roots) verbatim out of
  [`Talmud-2.1.6.mdx`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/Talmud-2.1.6.mdx)
  → new [`data/talmud_appendix1.json`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/talmud_appendix1.json)
  via new [`tools/parse_appendix1.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/parse_appendix1.py)
  (column semantics from the manual's own Tables 4/5/8, **not** the prior split-page legend,
  which had conflated Тип with the seṭ codes). Overlaid onto the Whitney spine in
  [`build_whitney_talmud.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/build_whitney_talmud.py)
  (join on col-4 «Список Уитни»; 730/745 rows → **730 Whitney roots carry the author's Ряд**,
  200 null = not in his catalog, 721 seṭ). `ryad_source`/`tip_source`/`set_source` = `"manual"`;
  new `tip`/`set_code`/`pada`; **dropped** `ryad_confidence`/`ryad_note`/`set_confidence` and the
  vowel-derivation as a value source (kept only for the audit). Reconciliation
  [`data/manual_reconciliation_report.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/manual_reconciliation_report.md):
  the retired derived Ряд disagreed with the author on **178/730** (75.6% agree) — un-indexed
  over-specification + the ṛ-nucleus divergence, the concrete case for the ruling. Regenerated
  the widget feed + Приложение 1 render (`Ряд*`/`seṭ*` → `Ряд`/`Тип`/`seṭ`, provenance note now
  cites the manual); AblautMachine/SetTree captions rewritten (author value, not "производная
  величина"); SetTree seats veṭ neutrally. Schema + CHANGELOG updated. JSON valid, no BOM.
- **§-addressable Ocherk + clickable `<ZRef>` (07-07-2026, H241, Opus 4.8 `claude-opus-4-8`):**
  layered on top of the merged concordance (PR #34) to make its §-refs actually navigable —
  per MG's 07-07 decision that the chips should link to a §-addressable edition.
  [`Zalizniak-Ocherk_29-11-20-aligned.mdx`](https://github.com/gasyoun/SanskritGrammar/blob/main/ZalizniakOcherk_1978/Zalizniak-Ocherk_29-11-20-aligned.mdx)
  now carries per-§ anchors `id="s<N>"` for all **242 §§** (§42 heading → `{#s42}`),
  injected by the idempotent
  [`tools/inject_ocherk_anchors.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/inject_ocherk_anchors.py).
  [`ZRef.jsx`](https://github.com/gasyoun/SanskritGrammar/blob/main/src/components/ZRef.jsx)
  upgraded from a text stub to a real link (parses the first § of a range, deep-links via
  `useBaseUrl` — base-correct for GH Pages **and** the samskrtam.ru deploy). Concordance
  §§ column converted to the same clickable chips. Two concurrent H241 sessions filled the
  concordance in parallel (PR #34 landed first, richer 3-col table — kept as canonical; my
  duplicate concordance fill discarded, only the orthogonal §-addressability kept). Verified:
  webpack Server/Client "Compiled successfully"; SSG count unchanged vs base (28 Node-24
  baseline).
- **Zalizniak §-concordance fill (07-07-2026, H241, Opus 4.8 `claude-opus-4-8`):** the
  "companion on top of Zalizniak" spine is complete —
  [`zalizniak-concordance.mdx`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/zalizniak-concordance.mdx)
  now maps each Talmud раздел (Введение, I–X, Прил. 1–2) to the Очерк §§ it formalizes,
  with a third «Ключевое соответствие» column stating *what* is formalized. §-ranges
  derived from a section-by-section reading of the 242-§ Очерк (subject-matter mapping,
  not heading-string matching — e.g. §II→§§ 50–57,60,62; §III→§ 63; §VII→§§ 16–20,31–46;
  §X→§§ 75–113). Inline `<ZRef sec="…"/>` companion chips placed at the head of all 13
  sections (`src/components/ZRef.jsx`, previously a call-site-less stub). All `TODO(H241)`
  markers in the concordance cleared. MDX compiles clean (build reaches SSG; see the
  pre-existing site-wide `DocItem` SSG break noted above, which is not from this change).
  Correspondences are a linguistic proposal for the author to verify (`:::note` says so).
- **Phase 2 interactive widgets (07-07-2026, H241, Opus 4.8 `claude-opus-4-8`):** all 5
  §-widgets built in `src/components/talmud/` and embedded in their sections —
  **AblautMachine** (§II, [talmud-02](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/talmud-02-cheredovanie.mdx)),
  **SetTree** seṭ/aniṭ decision tree (§III, [talmud-03](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/talmud-03-soedinitelnaya-i.mdx)),
  **ReduplicationSandbox** (§IV, [talmud-04](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/talmud-04-reduplikaciya.mdx)),
  **SandhiCollider** (§VII, [talmud-07](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/talmud-07-sandhi.mdx)),
  **HeteroclisisMap** (§X, [talmud-10](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/talmud-10-imennaya-sistema.mdx)).
  AblautMachine + SetTree are fed by a compact
  [`data/widget_roots.json`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/widget_roots.json)
  (28 KB, projected from `whitney_talmud.json` via
  [`tools/build_widget_data.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/build_widget_data.py)
  — not the 776 KB master, to keep the client bundle lean). Reduplication/heteroclisis use
  hand-verified curated traces (a general engine would over-claim on the irregular cases the
  text flags). Theme-aware styling via `styles.module.css` (infima vars, light+dark). Series
  table encoded once in `ablautSeries.js`. `npm run build` passes; all 5 widgets confirmed
  SSR-rendered into the built HTML. Derived Ряд/seṭ shown in widgets are labelled as gated
  proposals, never asserted as the author's.
- **Phase 3 enrichment crosswalk (07-07-2026, H241, Opus 4.8 `claude-opus-4-8`):**
  built [`data/whitney_talmud.json`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/whitney_talmud.json)
  — 930 Whitney verbal roots joined with the derived Ряд (from nucleus vowel, Table 2)
  + advisory seṭ (from p.p.p.) + `cohort` (DCS-freq) + the 15 verbatim Приложение-2
  nominal roots; reproducible via
  [`tools/build_whitney_talmud.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/build_whitney_talmud.py)
  (WhitneyRoots read-only). Приложение 1 now carries the first-cohort catalog table
  ([`tools/render_appendix1.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/render_appendix1.py))
  with a provenance `:::note`; 14 footnote proposals seeded
  ([`tools/gen_footnote_proposals.py`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/tools/gen_footnote_proposals.py)).
  Schema: [`data/whitney_talmud.schema.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/TolchelnikovTalmud_2026/data/whitney_talmud.schema.md).
- **Phase 0 scaffold (06-07-2026, H242, Fable 5 `claude-fable-5`):** book split into 15
  MDX pages (Введение, I–X, App 1–2, Заключение, concordance skeleton) with frontmatter;
  site wiring confirmed (folder already in `docusaurus.config.mjs` include + autogen
  sidebar; Zalizniak registered); `src/components/ZRef.jsx` stub (call-sites unplaced);
  `footnote-proposals/` (proposals.yml FN-0001 example + README with the Ivan-approval
  rule); `errata.yml` seeded empty + generated ERRATA.md; gitignore allow-list extended.
