# W1 exit note — rollback + known-consumer smoke evidence, three pilots

_Created: 24-09-2026 · Last updated: 24-09-2026_

Executed as [H5418](https://github.com/gasyoun/Uprava/blob/main/handoffs/H5418-Sonnet_SanskritGrammar_w1-exit-note-rollback-consumer-smoke_24.09.26.md),
closing the roadmap's [§10 "W1 exit note"](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/ROADMAP_SANSKRITGRAMMAR_ARCHITECTURE_2026_2027.md)
row. The roadmap row's own premise — "three executed pilots (H1911/H1912/H1913,
each closed with green V-gates)" — is **only two-thirds true**: verified against
`origin/main` and this repo's `CHANGELOG.md`, Slice B (H1912, KnauerFrazy_1908)
never advanced past its B0 baseline freeze. That gap is the main finding of
this note, not a defect in the note.

## Slice A — H1911 (delivery/publication-safety contracts)

| Step | Evidence |
|---|---|
| A0 baseline | [`docs/architecture/baseline/H1911_A0_BASELINE.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/architecture/baseline/H1911_A0_BASELINE.md) — 167 pytest passed, CI green on `main`; local `npm run build` red only from the known gitignored-archive discovery defect (the A3 target). |
| A3 (discovery constrained to tracked roots) | [PR #569](https://github.com/gasyoun/SanskritGrammar/pull/569) — registry-closed against H1911, A3-only. |
| A1/A2/A4 (uv workspace, `sg_tooling`, pipeline/work/provenance schemas, discovery deny layer + test seams) | [H2309](https://github.com/gasyoun/SanskritGrammar/blob/main/CHANGELOG.md) (CHANGELOG 0.121.6) — 265 tracked `.mdx`, 0 denied by the new layer; 68 Node + 38 Python tests added. |
| A5/A6 (neutral rights footer, same-SHA Pages gate + deliberately-red control) + independent adjudication | [H2528](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/architecture/H2528_DUAL_RUN_RECONCILIATION.md) — Codex Sol vs Fable 5 dual-run reconciled, 1 identical/4 equivalent/5 conflicting/5 net-new, every conflict has a named winner. Reconciled tree: `uv sync --frozen` pass, 261 pytest, 68 Node tests, `npm run build` SUCCESS with the neutral footer rendered. Delivered [PR #849](https://github.com/gasyoun/SanskritGrammar/pull/849) (merged), repaired by [PR #850](https://github.com/gasyoun/SanskritGrammar/pull/850). |
| Remote same-SHA gate evidence | Green run [#928](https://github.com/gasyoun/SanskritGrammar/actions/runs/31334518093) on `132a620d`: `deployment-sha.txt` matched `github.sha`, deploy consumed the uploaded artifact with no rebuild. Deliberately-red control run [#930](https://github.com/gasyoun/SanskritGrammar/actions/runs/31334671187) on the same SHA: planted failure stopped identity/staging/Pages/deploy as designed. |
| Rollback / known-consumer smoke, Slice-A-specific | **Not a distinct rehearsal.** Slice A ships zero content generators (CHANGELOG 0.121.6, A1 note: "Slice A registers zero content generators") — there is no content cutover to roll back. Its equivalent of "known-consumer smoke" is the same-SHA Pages gate's negative control above: a real build/deploy path that fails closed when a required gate is red. |

**Verdict:** Slice A is fully executed and evidenced, but the roadmap's phrase
"joint rollback + known-consumer smoke" does not literally apply to it — there
was no content migration to roll back. Treat its evidence class as
*infra-gate PASS + negative-control PASS*, not *rollback rehearsal PASS*.

## Slice B — H1912 (KnauerFrazy_1908 vertical pilot) — INCOMPLETE

| Step | Evidence |
|---|---|
| B0 (pre-cutover invariants frozen) | [`docs/architecture/baseline/H1912_B0_INVARIANTS.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/architecture/baseline/H1912_B0_INVARIANTS.md) + [`h1912_b0_invariants.json`](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/architecture/baseline/h1912_b0_invariants.json), frozen at `origin/main acc3741` (release 0.119.0): 11 tracked files hashed, both site routes + 20 anchors, errata 30→25 merged, parse audit KN-1..KN-214 (210 CONFIRMED/4 QUESTIONABLE), 7 code-level consumers named. |
| B1–B4 (actual migration behind `sg_tooling`, cutover, rollback rehearsal, consumer smoke) | **Never executed.** The B0 doc states outright: "B1–B4 consume the Slice A shared contracts... **None of those exist on `origin/main`** [as of 06-08-2026]... the rest of the pilot resumes once the Slice A remainder ships." `CHANGELOG.md` 0.120.0 repeats: "B1–B4 blocked: ... the remainder is re-minted as a fresh handoff in Uprava." Slice A's remainder (H2309/H2528) shipped 09–22-08-2026 — **after** that, no CHANGELOG entry, no doc, and no `docs/architecture/` reconciliation memo for Knauer B1–B4 exists anywhere in this repo (verified: `grep -n "Knauer" CHANGELOG.md` after 0.120.0 returns only unrelated errata/parse-audit rows). |

**Verdict: Slice B has NO rollback or known-consumer smoke evidence to
collect, because that work was never done.** The roadmap row's premise is
factually wrong for this pilot. This is not a human-gated blocker for
*this* note (writing an honest note needs no permission) — it is a genuine
open item this note surfaces for whoever picks W2/W3: either mint and run
Knauer B1–B4 before treating "W1" as closed in spirit, or explicitly
descope Slice B from the W1 exit claim.

## Slice C — H1913 (SG-MO-021 future-tense pilot)

| Step | Evidence |
|---|---|
| C0 baseline | [`docs/architecture/baseline/H1913_C0_INVARIANTS.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/architecture/baseline/H1913_C0_INVARIANTS.md) — full `published_numbers` block frozen (future 21 556/523 721 finite = 4.12%), DCS snapshot pin `04e0778` + sha256, 5 code-level consumers, issue-563 gate clean. |
| C1–C4 (pipeline behind `sg_tooling`, hard cutover, dual-run reconciliation) | [`docs/architecture/H1913_DUAL_RUN_RECONCILIATION.md`](https://github.com/gasyoun/SanskritGrammar/blob/main/docs/architecture/H1913_DUAL_RUN_RECONCILIATION.md) — override lane (OpenCode, PR [#874](https://github.com/gasyoun/SanskritGrammar/pull/874)) vs independent lane (Codex `GPT-5`) reconciled: 1 identical/3 equivalent/2 conflicting/3 net-new, every area adjudicated with a named winner. |
| **Rollback rehearsal** | Verification transcript item 7: "Rollback rehearsal from pre-cutover commit `d00f9dca` — **PASS**: the legacy generator reproduced its committed outputs with no diff." |
| **Known-consumer smoke** | "Exact sibling-repository search for `sangram/articles/future` found no external code/path consumer... Internal consumers" row: article links, visa source/index, editorial ledger refresh, `article_validate.py`, denominator validation, and discovery tests all updated in the same cutover (independent lane, adopted as winner). |
| Full V-C gate (25-08-2026) | `uv run sg pipeline check sg-mo-021-future` PASS · `uv run sg pipeline run sg-mo-021-future` ×2 byte-stable PASS · `pytest tests/golden/sg_mo_021_future tests/integration/test_sg_mo_021_future_pipeline.py` PASS · `article_validate.py --all` PASS · `check_claims_consistency.py` PASS · `npm run build` PASS · rollback rehearsal PASS. |

**Verdict:** Slice C is the only one of the three that literally matches the
roadmap's phrase — a real content cutover with a rehearsed rollback and a
real consumer census, both green.

## Net conclusion for W2

- **Ready as precedent for W2 (core factories migration):** Slice C's
  pattern — pin invariants, build behind `sg_tooling`, dual-run reconcile,
  rehearse rollback from the pre-cutover commit, census + update every named
  consumer, hard cutover. This is the evidence-driven order §10/W2 already
  points at.
- **Slice A** supplies the shared contracts W2 depends on, fully shipped and
  gated, but is not itself a rollback precedent (no content to roll back).
- **Slice B is an open gap, not a closed pilot.** Before W2's launch ruling
  treats "three pilots done" as true, either (a) mint and run Knauer B1–B4
  to get a real second rollback+consumer-smoke data point beside Slice C, or
  (b) have the human ruling explicitly acknowledge W2 is proceeding on
  *one* fully rehearsed pilot (C) plus infra (A), not two.

_Dr. Mārcis Gasūns_
